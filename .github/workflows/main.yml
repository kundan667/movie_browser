name: first-workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run a sample github actions
        run: this git hib actions runs successfully
